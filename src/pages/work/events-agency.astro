---
import CaseStudyPage from '../../components/work/CaseStudyPage.astro';
import { WORK_PROJECTS } from '../../lib/content';

const project = WORK_PROJECTS.find((item) => item.slug === 'events-agency');
if (!project) throw new Error('events-agency project not found in WORK_PROJECTS');
---

<CaseStudyPage project={project} />

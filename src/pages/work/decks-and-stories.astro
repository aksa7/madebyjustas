---
import CaseStudyPage from '../../components/work/CaseStudyPage.astro';
import { WORK_PROJECTS } from '../../lib/content';

const project = WORK_PROJECTS.find((item) => item.slug === 'decks-and-stories');
if (!project) throw new Error('decks-and-stories project not found in WORK_PROJECTS');
---

<CaseStudyPage project={project} />
